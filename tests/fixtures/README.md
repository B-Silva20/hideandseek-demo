# 本地测试案件

将《占星术杀人魔法.txt》存放在 `private/` 目录中，仅用于本地开发测试。

- `private/` 中的案件文件已被 `.gitignore` 排除。
- Vite 开发服务器禁止访问 `tests/`，后端未提供任何测试文件的静态访问接口。
- 前端不得导入此目录的文本；生产构建仅输出前端资源和后端代码。
- 公开版本仅使用原创或已获得授权的短篇案件，后续步骤再接入。
