# 第二步：案件输入

首页提供预置案件、粘贴文本、TXT 文件三种入口。点击“准备案件文本”后进行清理和校验，统一产生 `{ text, sourceType }`，其中 `sourceType` 为 `preset | text | txt`。结果交给 App 的 `caseInput` 状态，供后续解析功能接入；本步没有解析 API，不调用大模型，也不保存输入进度，刷新页面会清空。

限制：至少 100 个非空白字符；清理后最多 200,000 个 Unicode 字符；TXT 最大 5 MiB（界面标为 5 MB）。会处理 BOM、换行和零宽字符，拒绝空白、纯符号、过短、超长、替换乱码字符及明显二进制控制字符。该校验不判断案件语义是否完整。

文件按严格 UTF-8 优先、失败后严格 GB18030 解码，兼容 GBK。编码自动检测存在歧义，因此提供手动编码选择和文本开头预览。无法解码、空文件、扩展名错误和读取异常均有重试提示。

预置案件使用现有 `tests/fixtures/private/占星术杀人魔法.txt`。新增 `GET /api/cases/preset` 专门读取固定文件并返回原始字节，浏览器统一解码。该接口不依赖模型环境变量，也不接受任意文件路径；后端运行时，该固定案件可通过这个接口获取。直接静态访问 tests 目录仍受限，文件仍不进入 Git 或前端构建产物。其他机器运行预置入口时需在相同位置放置文件，缺失时页面提示改用粘贴或上传。

上述预置入口说明取代第一步文档中关于该文件没有任何访问接口的描述；第一步文档保留为阶段记录。本步尚未公开部署，也未推送到 GitHub。

验证：`npm test`、`npm run lint`、`npm run build`。浏览器已验收三个入口及 sourceType；真实预置 TXT 按 GB18030 解码，清理后 183,291 字符。
