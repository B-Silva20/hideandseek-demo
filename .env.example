# Backend only. Copy to .env and fill in values locally.
# Never add VITE_ to these names or put secret values in browser requests/logs.
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
DATABASE_URL=

# 预置案件的凶手姓名（可选）。必须与预置案件 Briefing 中的人物姓名完全一致。
# 留空时预置案件的「申请逮捕」一律返回「无法判定」，不会猜测答案。
PRESET_CULPRIT=
